import { NgModule } from '@angular/core';
import { WebviewClientService } from './webview-client.service';

@NgModule({
  providers: [
    WebviewClientService
  ]
})
export class WebviewClientModule { }
